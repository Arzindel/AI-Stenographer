# AI-Stenographer
My goal was to create an equivalent to mobile Speech-to-Text keyboards that doesn't simply writes down exactly you say, but formats the text and follows instructions given via speech as well. It will simulate keyboard key presses in whichever text box is selected.
